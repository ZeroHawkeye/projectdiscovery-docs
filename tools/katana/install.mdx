---
title: "安装 Katana"
description: "了解如何安装 Katana"
sidebarTitle: "安装"
---
<Tabs>
  <Tab title="Go">
    <Note> 在终端中输入以下命令，使用 Go 安装 ProjectDiscovery 的 Katana。 </Note>
    ```bash
    go install github.com/projectdiscovery/katana/cmd/katana@latest
   ```
  </Tab>
  <Tab title="Docker">
  <Note> 在终端中输入以下命令，使用 Docker 安装 ProjectDiscovery 的 Katana。 </Note>
    安装/更新 Docker 到最新版本
    ```bash
   docker pull projectdiscovery/katana:latest
    ```
  </Tab>
    <Tab title="GitHub">
   <Note> 在终端中输入以下命令，使用 GitHub 安装 ProjectDiscovery 的 Katana。 </Note>
    ```bash
   go install github.com/projectdiscovery/katana/cmd/katana@latest
    ```
  </Tab>
    <Tab title="Ubuntu">
    对于运行 Ubuntu，我们建议安装以下先决条件

    ```sh
    sudo apt update
    sudo snap refresh
    sudo apt install zip curl wget git
    sudo snap install golang --classic
    wget -q -O - https://dl-ssl.google.com/linux/linux_signing_key.pub | sudo apt-key add - 
    sudo sh -c 'echo "deb http://dl.google.com/linux/chrome/deb/ stable main" >> /etc/apt/sources.list.d/google.list'
    sudo apt update 
    sudo apt install google-chrome-stable
    ```
  </Tab>
  <Tab title="Binary">
    ```bash
    https://github.com/projectdiscovery/katana/releases
    ```

    <Tip>
    - 下载适用于您操作系统的最新二进制文件。
    - 解压文件以运行二进制文件。
    </Tip>

  </Tab>
</Tabs>

## 安装说明
  - Katana 需要最新版本的 [**Go**](https://go.dev/doc/install)
  - 将 Go bin 路径添加到系统路径中。在 OSX 或 Linux 中，在终端使用
  
  ```
  echo export $PATH=$PATH:$HOME/go/bin >> $home/.bashrc
  source $home/.bashrc
  ```
  - 要在 Windows 中添加 Go bin 路径，[点击此链接获取说明。](https://www.architectryan.com/2018/03/17/add-to-the-path-on-windows-10/)
  - 二进制文件将位于 `$home/go/bin/katana`  
